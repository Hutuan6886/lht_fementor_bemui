export type ExtensionType = {
  name: string;
  description: string;
  logo: string;
  isActive: boolean;
};

export type FilterType = {
  label: string;
  isActive: boolean;
};

export type ThemeType = "light" | "dark";
